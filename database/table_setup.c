/*#include <my_global.h>
#include <mysql.h>*/

/*int main(int argc, char **argv)
{

  MYSQL *conn;

  conn = mysql_init(NULL);
  mysql_real_connect(conn, "localhost", "root", "lolita1168", "cgroups", 0, NULL, 0);

  mysql_query(conn, "CREATE TABLE cgroupsContiki(id INT primary key, \
						name VARCHAR(25),\
						ipv4 VARCHAR(25),\
						port VARCHAR(25),\
						mac VARCHAR(25))");

  mysql_close(conn);
}
*/

